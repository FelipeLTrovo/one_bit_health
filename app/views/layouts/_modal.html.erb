<div class="modal fade" id="share-modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLongTitle" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLongTitle"><%= modal_title %></h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">

      <%= form_with(model: Shared, local: true) do |form| %>
        <div class="form-group">
          <label for="">Compartilhar com</label>
          <%= form.select :user_id, User.all.pluck(:email, :id), { prompt: "Selecione o usuário" }, { class: 'form-control' } %>
        </div>
      <% end %>

      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-primary">Compartilhar</button>
      </div>
    </div>
  </div>
</div>
